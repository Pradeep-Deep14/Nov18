def mul(x=10,y):
    return (x*y)
print(mul(5,15))


#when defining a function with default
#  parameter values, 
# the parameters with default
#  values must come after parameters
#  without default values